<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Potluck</title>
	<link rel="stylesheet" href="/css/normalize.css">
	<link rel="stylesheet" href="/css/style.css">
</head>
<body>
	<h1>Welcome to <span class="gradientWord">Potluck</span>!</h1>
	<p class="singleLine">Please login or sign up below!</p> 
	<% if (locals.messages.errors) { %>
        <% messages.errors.forEach( el => { %>
            <div class="alert alert-danger"><%= el.msg %></div>
        <% }) %>    
    <% } %>
	<span> </span>
    <% if (locals.messages.info) { %>
        <% messages.info.forEach( el => { %>
            <div class="alert alert-danger"><%= el.msg %></div>
        <% }) %>    
    <% } %>
    <form action="/login" method="POST">
        <input type="email" name="email" placeholder="Email" class="typedInput">
        <input type="password" name="password" placeholder="Password" class="typedInput">
        <input type="submit" value="Login" class="btn">
		<a href="/signup" class="btn">Sign Up</a>
    </form>
</body>
</html>